import {call, put, takeEvery} from 'redux-saga/effects';
//import * as Map from '../apis/map';

// main Map saga generators
export default function* mapsagas() {
  //yield takeEvery("MAP_INIT", mapInit);
}